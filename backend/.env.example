# DeepSeek API Key (server-side, used as fallback for free tier)
DEEPSEEK_API_KEY=sk-your-deepseek-api-key-here

# Server secret for encrypting user BYOK keys (change in production)
SERVER_SECRET=change-this-to-a-random-string-in-production

# Server port
PORT=8000
